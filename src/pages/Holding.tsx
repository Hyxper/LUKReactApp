const Holding = ({ message }: { message: string }) => {
  return (
    <div style={{ padding: '20px', textAlign: 'center'}}>
      <h1>{message}</h1>
      <p>This page is under construction. Please check back later.</p>
    </div>
  );
};

export default Holding;
